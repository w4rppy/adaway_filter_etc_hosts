## adaway_filter_etc_hosts
{deploy|remove|upgrade} rules from https://adaway.org/hosts.txt into /etc/hosts

### usage
`sudo adaway_hosts.sh deploy`
`sudo adaway_hosts.sh remove`
`sudo adaway_hosts.sh upgrade`
